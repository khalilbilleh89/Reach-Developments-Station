# Reach Developments Station — MVP 1.0 production dependencies.
#
# Dependency policy (docs/ENGINEERING_RULES.md): this file is the canonical
# source of truth for backend runtime dependencies. Versions are pinned exactly
# so that local, CI and Render installs are deterministic. Every addition must
# be justified in the pull request template.

alembic==1.19.1
# Argon2id password hashing. Authentication is the one place a hand-rolled
# scheme is unacceptable, so this is a justified production dependency
# (PR-MVP-01). Used directly — no Passlib, no auth framework.
argon2-cffi==25.1.0
fastapi==0.141.1
psycopg[binary]==3.3.4
pydantic-settings==2.15.0
sqlalchemy==2.0.52
uvicorn==0.52.4
