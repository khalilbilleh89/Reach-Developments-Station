"""
strategy_execution_package

Strategy Execution Package Generator (PR-V7-07).

Translates recommended strategy outputs into structured, execution-ready
action packages.  Read-only — no source records are mutated.

Public API
----------
  GET /api/v1/projects/{id}/strategy-execution-package
  GET /api/v1/portfolio/execution-packages
"""
