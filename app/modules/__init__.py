"""Business domain modules.

Each module owns its models, schemas, service logic and routes. Modules may use
``app.core`` and may call another module's public service functions, but never
reach into another module's persistence internals.
"""
